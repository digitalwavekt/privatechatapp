import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.digitalwave.pvchat",
  "appName": "PVChat",
  "webDir": "build",
  "server": {
    "androidScheme": "https"
  }
};

export default config;
